# OpenHands Monitor Bot

## Описание проекта

OpenHands Monitor Bot — это Telegram-бот, предназначенный для мониторинга задач на платформе OpenHands и отправки уведомлений в реальном времени об изменениях статуса задач. Он непрерывно опрашивает API OpenHands для получения обновлений о беседах/задачах и оповещает пользователей о:

- **Новых задачах**, которые создаются
- **Изменениях статуса** (например, с "running" на "completed")
- **Завершении задач**

Этот бот помогает командам быть в курсе своих задач OpenHands без необходимости постоянно проверять платформу вручную.

### Ключевые возможности
- **Мониторинг в реальном времени**: Непрерывно опрашивает API OpenHands для получения обновлений задач
- **Уведомления в Telegram**: Отправляет мгновенные оповещения в настроенный чат Telegram
- **Отслеживание статуса**: Сохраняет состояние для обнаружения изменений между циклами опроса
- **Обработка ошибок**: Грациозно обрабатывает ошибки API и проблемы с сетью
- **Настраиваемый опрос**: Регулируемый интервал для проверки обновлений

## Инсталляция

### Шаг 1: Клонирование репозитория

Клонируйте репозиторий на ваш локальный компьютер:

```bash
git clone https://github.com/bughouse-wizzard/openhands-monitor-bot.git
cd openhands-monitor-bot
```

### Шаг 2: Установка зависимостей

Установите все необходимые пакеты Python с помощью pip:

```bash
pip install -r requirements.txt
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта с необходимыми переменными окружения:

1. **Скопируйте пример файла окружения:**
   ```bash
   cp .env.example .env
   ```

2. **Отредактируйте файл `.env`** с вашей конфигурацией (подробности см. в разделе Конфигурация ниже):
   - `TELEGRAM_TOKEN` - Токен вашего Telegram-бота (обязательно)
   - `CHAT_ID` - ID вашего чата Telegram (обязательно)
   - `OPENHANDS_API_URL` - URL вашего API OpenHands (опционально, по умолчанию `http://host.docker.internal:3000`)

## Использование

Чтобы запустить OpenHands Monitor Bot, выполните:

```bash
python bot.py
```

Бот будет:
1. Проверять, что все необходимые переменные окружения установлены
2. Отправлять уведомление о запуске в ваш чат Telegram
3. Начинать опрашивать API OpenHands с настроенным интервалом
4. Отправлять уведомления о новых задачах и изменениях статуса

## Конфигурация

Бот требует установки следующих переменных окружения в файле `.env`:

### Обязательные переменные окружения

- **`TELEGRAM_TOKEN`**: Токен API вашего Telegram-бота (получите у [@BotFather](https://t.me/botfather))
- **`CHAT_ID`**: ID чата Telegram, куда будут отправляться уведомления

### Опциональные переменные окружения

- **`OPENHANDS_API_URL`**: Базовый URL для API OpenHands (по умолчанию: `http://host.docker.internal:3000`)
- **`POLL_INTERVAL`**: Интервал опроса в секундах (по умолчанию: `5`)

### Пример файла `.env`

```env
# Обязательные переменные
TELEGRAM_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
CHAT_ID=987654321

# Опциональные переменные
OPENHANDS_API_URL=http://localhost:3000
POLL_INTERVAL=5
```

**Примечание**: В инструкциях задачи упоминаются `TELEGRAM_BOT_TOKEN` и `OPEN_HANDS_API_URL`, но фактический код использует `TELEGRAM_TOKEN` и `OPENHANDS_API_URL`. Используйте имена переменных, указанные выше, для правильной работы.

## Описание API

### Основные функции

#### `send_telegram_message(message: str) -> bool`
**Расположение**: `bot.py`

**Описание**: Отправляет сообщение в настроенный чат Telegram.

**Параметры**:
- `message` (str): Текст сообщения для отправки.

**Возвращает**:
- `bool`: True, если сообщение успешно отправлено, False в противном случае.

**Обработка ошибок**:
- Повторяет попытку 3 раза с задержкой 2 секунды
- Логирует ошибки при неудаче
- Вызывает исключение после исчерпания попыток

#### `fetch_conversations() -> list[dict]`
**Расположение**: `bot.py`

**Описание**: Получает беседы из API OpenHands.

**Параметры**: Нет

**Возвращает**:
- `list[dict]` или `[]`: Список бесед или пустой список при ошибке.

**Обработка ошибок**:
- Грациозно обрабатывает HTTP-ошибки
- Возвращает пустой список при сбоях сети
- Логирует детали ошибок

#### `poll_and_notify() -> None`
**Расположение**: `bot.py`

**Описание**: Основной цикл опроса для мониторинга бесед.

**Параметры**: Нет

**Возвращает**: Нет (работает бесконечно)

**Поведение**:
- Опрашивает API с настроенным интервалом
- Сравнивает текущее состояние с предыдущим
- Отправляет уведомления об изменениях
- Очищает старые состояния бесед

#### `main() -> None`
**Расположение**: `bot.py`

**Описание**: Основная точка входа для OpenHands Monitor Bot.

**Параметры**: Нет

**Возвращает**: Нет

**Поведение**:
- Проверяет переменные окружения
- Отправляет уведомление о запуске
- Запускает цикл опроса
- Обрабатывает корректное завершение работы

### Модуль словаря (`map_maker.py`)

#### `get_definitions(word: str, custom_dict: dict = None) -> list`
**Описание**: Получает определения слова из словаря.

**Параметры**:
- `word` (str): Слово для поиска. Автоматически преобразуется в строку, если еще не является строкой.
- `custom_dict` (dict, опционально): Пользовательский словарь для использования вместо стандартного словаря.

**Возвращает**:
- `list`: Список определений. Пустой список, если слово не найдено.

**Примеры**:
```python
from map_maker import get_definitions

# Поиск в стандартном словаре
definitions = get_definitions("apple")
# Возвращает: ['A fruit that grows on trees', 'A technology company founded by Steve Jobs']

# Поиск в пользовательском словаре
custom_dict = {"python": ["Мой любимый язык программирования"]}
definitions = get_definitions("python", custom_dict)
# Возвращает: ['Мой любимый язык программирования']

# Слово не найдено
definitions = get_definitions("nonexistent")
# Возвращает: []
```

**Обработка ошибок**:
- Входные данные не-строки преобразуются в строки
- Пустые строки возвращают пустые списки
- Значения None преобразуются в строку "none"
- Возвращает копию списка для предотвращения изменения оригинального словаря

### Стандартный словарь

Стандартный словарь (`STANDARD_DICTIONARY`) содержит:

| Слово | Определения |
|------|-------------|
| `apple` | 1. "A fruit that grows on trees"<br>2. "A technology company founded by Steve Jobs" |
| `python` | 1. "A high-level programming language"<br>2. "A large constricting snake" |
| `openhands` | "A platform for AI development and collaboration" |
| `test` | 1. "A procedure intended to establish the quality, performance, or reliability of something"<br>2. "An examination of someone's knowledge or proficiency" |
| `hello` | 1. "A greeting or expression of goodwill"<br>2. "Used to attract attention" |
| `world` | 1. "The earth, together with all of its countries and peoples"<br>2. "A particular region or group of countries" |

### Формат уведомлений Telegram

#### Уведомление о новой задаче
```
🆕 New Task Started: {title} (ID: {conv_id})
```

#### Уведомление об обновлении статуса
```
🔄 Task Status Update: {title} is now {status}.
```

#### Уведомление о запуске
```
🤖 OpenHands Monitor Bot is online and starting to poll.
```

## Тестирование

Проект включает в себя комплексную систему тестирования с высоким покрытием кода (93%). Тесты написаны с использованием `pytest` и покрывают все основные сценарии использования.

### Запуск тестов

#### Запуск всех тестов
```bash
python -m pytest tests/ -v
```

#### Запуск тестов с покрытием кода
```bash
python -m pytest tests/ --cov=bot --cov=map_maker --cov-report=html --cov-report=term
```

#### Запуск тестов для конкретного модуля
```bash
# Тесты для bot.py
python -m pytest tests/test_bot.py -v

# Тесты для map_maker.py
python -m pytest tests/test_map_maker.py -v
```

### Структура тестов

#### Тесты для bot.py (`tests/test_bot.py`)
- **Модульные тесты**: Проверка структуры модуля и инициализации переменных
- **Тесты конфигурации**: Проверка переменных окружения и настроек
- **Тесты функций**: Тестирование основных функций (`send_telegram_message`, `fetch_conversations`)
- **Тесты цикла опроса**: Проверка логики мониторинга и уведомлений
- **Тесты обработки ошибок**: Проверка грациозной обработки ошибок API и сети

#### Тесты для map_maker.py (`tests/test_map_maker.py`)
- **Базовые тесты**: Проверка основной функциональности функции `get_definitions`
- **Тесты граничных случаев**: Обработка пустых строк, пробелов, специальных символов
- **Тесты пользовательских словарей**: Работа с пользовательскими словарями
- **Тесты Unicode**: Поддержка Unicode символов и нормализация
- **Тесты производительности**: Проверка работы с большими словарями
- **Тесты типов данных**: Обработка различных типов входных данных

### Покрытие тестами

| Модуль | Строк кода | Непокрыто | Покрытие |
|--------|------------|-----------|----------|
| `bot.py` | 76 | 6 | 92% |
| `map_maker.py` | 10 | 0 | 100% |
| **Всего** | **86** | **6** | **93%** |

### Отчет о покрытии

После запуска тестов с опцией `--cov-report=html` будет создан HTML отчет в директории `htmlcov/`. Открыв файл `htmlcov/index.html` в браузере, вы можете увидеть детальную информацию о покрытии кода тестами.

### Тестирование в CI/CD

Для интеграции в CI/CD пайплайны можно использовать следующие команды:

```bash
# Запуск тестов с минимальным выводом
python -m pytest tests/ --tb=short

# Запуск тестов с выходным кодом для CI
python -m pytest tests/ --junitxml=test-results.xml

# Проверка покрытия с порогом
python -m pytest tests/ --cov=bot --cov=map_maker --cov-fail-under=90
```

### Написание новых тестов

При добавлении нового функционала рекомендуется следовать следующим принципам:

1. **Тестируйте все публичные функции**: Каждая публичная функция должна иметь соответствующие тесты
2. **Покрывайте граничные случаи**: Тестируйте пустые значения, некорректные входные данные, ошибки
3. **Используйте моки для внешних зависимостей**: Для тестирования функций, взаимодействующих с внешними API
4. **Поддерживайте высокое покрытие**: Стремитесь к покрытию не менее 90% для новых функций

### Пример теста

```python
import pytest
from map_maker import get_definitions

def test_basic_functionality():
    """Тест базовой функциональности."""
    result = get_definitions("apple")
    expected = [
        "A fruit that grows on trees",
        "A technology company founded by Steve Jobs"
    ]
    assert result == expected
```

## Разработка

### Структура проекта

```
openhands-monitor-bot/
├── bot.py                    # Основной модуль мониторинга
├── map_maker.py              # Модуль работы со словарями
├── requirements.txt          # Зависимости Python
├── tests/                    # Тесты
│   ├── test_bot.py          # Тесты для bot.py
│   ├── test_bot_focused.py  # Фокусированные тесты для bot.py
│   └── test_map_maker.py    # Тесты для map_maker.py
├── .env.example             # Пример файла окружения
├── docker-compose.yml       # Конфигурация Docker Compose
└── Dockerfile              # Docker образ
```

### Установка для разработки

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/bughouse-wizzard/openhands-monitor-bot.git
   cd openhands-monitor-bot
   ```

2. **Установите зависимости для разработки**:
   ```bash
   pip install -r requirements.txt
   ```

3. **Настройте виртуальное окружение** (рекомендуется):
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/Mac
   # или
   venv\Scripts\activate     # Windows
   pip install -r requirements.txt
   ```

### Стиль кода

Проект следует стандартам PEP 8 для Python кода. Для проверки стиля кода можно использовать:

```bash
# Установите flake8
pip install flake8

# Проверьте стиль кода
flake8 bot.py map_maker.py tests/
```

### Коммиты

При внесении изменений используйте осмысленные сообщения коммитов:

```
feat: Добавлена новая функция для обработки ошибок
fix: Исправлена ошибка в функции send_telegram_message
test: Добавлены тесты для функции get_definitions
docs: Обновлена документация в README.md
```

## Деплой

### Docker

Проект включает Dockerfile для контейнеризации:

```bash
# Сборка образа
docker build -t openhands-monitor-bot .

# Запуск контейнера
docker run -d --name openhands-bot \
  -e TELEGRAM_TOKEN=your_token \
  -e CHAT_ID=your_chat_id \
  openhands-monitor-bot
```

### Docker Compose

Для запуска с Docker Compose используйте:

```bash
# Копируйте пример файла окружения
cp .env.example .env
# Отредактируйте .env с вашими настройками

# Запустите сервис
docker-compose up -d
```

### Мониторинг

Для мониторинга работы бота:

1. **Логи**: Проверяйте логи контейнера
   ```bash
   docker logs openhands-bot
   ```

2. **Статус**: Проверяйте статус контейнера
   ```bash
   docker ps | grep openhands-bot
   ```

3. **Перезапуск**: При необходимости перезапустите контейнер
   ```bash
   docker restart openhands-bot
   ```

## Устранение неполадок

### Общие проблемы

#### Бот не отправляет сообщения
1. Проверьте, что `TELEGRAM_TOKEN` и `CHAT_ID` установлены корректно
2. Убедитесь, что бот добавлен в чат и имеет права на отправку сообщений
3. Проверьте логи на наличие ошибок Telegram API

#### Ошибки подключения к API OpenHands
1. Проверьте, что `OPENHANDS_API_URL` указывает на корректный URL
2. Убедитесь, что API доступен из сети, где запущен бот
3. Проверьте логи на наличие сетевых ошибок

#### Высокая загрузка CPU/памяти
1. Увеличьте `POLL_INTERVAL` для уменьшения частоты опроса
2. Проверьте, нет ли утечек памяти в цикле опроса
3. Рассмотрите использование более легковесных библиотек

### Логирование

Бот использует стандартное логирование Python. Уровень логирования можно настроить через переменную окружения:

```bash
export LOG_LEVEL=DEBUG  # DEBUG, INFO, WARNING, ERROR
```

## Лицензия

Проект распространяется под лицензией MIT. Подробности см. в файле [LICENSE](LICENSE).

## Вклад в проект

Мы приветствуем вклад в проект! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения информации о процессе внесения изменений.

## Поддержка

Если у вас возникли проблемы или вопросы:
1. Проверьте [документацию](TECHNICAL_DOCUMENTATION.md)
2. Создайте issue в репозитории
3. Обратитесь к разработчикам через Telegram

## Благодарности

- Команде OpenHands за создание платформы
- Разработчикам python-telegram-bot за отличную библиотеку
- Всем контрибьюторам проекта



